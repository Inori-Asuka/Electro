# 示例配置：ViT Large + 方案A (GAP + MLP)
defaults:
  - config

exp: dinov3_vit_large_gap

dinov3:
  backbone_type: vit 
  model_path: /data/wly/ckpt/dinov3_vitl16 /model.safetensors
  pooling: gap 
  head_type: mlp

